# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build outputs (not needed on server)
dist
dist-ssr
coverage

# Development tools and scripts (not needed on server)
scripts/
docs/

# Environment and config files (not needed on server)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.local

# Development and testing
/cypress/videos/
/cypress/screenshots/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript
*.tsbuildinfo

# Temporary files
*.tmp
*.temp

# Package manager files (keep package.json but ignore lock files for deployment)
# Note: You might want to include lock files in your repo for consistency
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Example/demo directories (if any)
stellar-copy-paste-genesis-main/

# SEO and documentation files (optional - can be included in repo)
# Uncomment if you don't want these in your repository:
# SEO_CHECKLIST.md
# public/humans.txt
# public/.well-known/

# Generated sitemap (auto-generated during build)
public/sitemap.xml
dist/sitemap.xml

# Security files with sensitive info (if they contain real contact info)
# public/.well-known/security.txt

# PWA files (these should be included in repo)
# public/manifest.json
